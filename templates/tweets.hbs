<h1>Twitter</h1>
<div class="row">
    <div class="col-md-6">
      {{#link-to "new-tweet"}}<span class="btn btn-primary">Send a Tweet?</span>{{/link-to}}

      {{outlet}}
    <ul>
      {{#each}}
      <li>{{text}}</li>
      {{/each}}
    </ul>
  </div>
</div>
